import sys

first = set(["White", "Black", "Red"])
seccond = set(["Red", "Green"])



for i in range(len(first)):
    temp = list(first)[i]
    
    if (temp in seccond):
        continue
    else:
        print(temp)
        













































